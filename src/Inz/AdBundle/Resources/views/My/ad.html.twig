{% extends "::base.html.twig" %}
{% block content_title %}Moje ogłoszenia{% endblock %}

{% block content %}
	{% if entities is iterable %}
        <ul>
        {% for entity in entities %}
            <li><a href="{{ path('link_ad', { 'id': entity.id }) }}">{{ entity.title }}</a></li>
        {% endfor %}
        <ul>
    {% else %}
    	Nie dodałeś jeszcze żadnego ogłoszenia.
    {% endif %}
{% endblock %}